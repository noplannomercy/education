# AI Personal Finance - 제품 요구사항 정의서 (PRD)

## 1. 프로젝트 개요

### 1.1 목적
AI 기반 개인 재무 관리 앱. 거래 기록, AI 지출 패턴 분석, 예산 관리, 저축 조언, 자동 카테고리 분류.

### 1.2 기술 스택
- 프레임워크: Next.js 14 (App Router)
- 언어: TypeScript
- 데이터베이스: PostgreSQL + Drizzle ORM
- UI: shadcn/ui + Tailwind CSS
- 차트: Recharts
- AI: AI SDK + Open Router
- 모델: `anthropic/claude-haiku-4.5`

### 1.3 대상 사용자
- 개인 지출을 관리하고 싶은 사람
- 저축 목표를 달성하고 싶은 사람
- 지출 패턴을 분석하고 싶은 사람
- 예산을 효율적으로 관리하고 싶은 사람

---

## 2. 기능 (10개)

### 기능 1: 거래 내역 CRUD
거래 기록 관리
- 금액
- 카테고리 (식비, 교통, 쇼핑, 공과금, 기타)
- 설명
- 날짜
- 유형 (수입/지출)
- 결제 수단 (현금, 카드, 계좌이체)

### 기능 2: 카테고리 관리
카테고리 생성/수정/삭제
- 카테고리 이름
- 색상
- 아이콘
- 월 예산 설정

### 기능 3: 예산 설정
월별/카테고리별 예산
- 총 예산 설정
- 카테고리별 예산 배분
- 예산 사용률 추적
- 초과 경고

### 기능 4: 통계 대시보드
재무 통계 시각화
- 월별 수입/지출 (Line Chart)
- 카테고리별 지출 (Pie Chart)
- 일별 지출 추이 (Bar Chart)
- 저축률
- 예산 달성률

### 기능 5: 검색 & 필터
거래 내역 검색
- 날짜 범위
- 카테고리
- 금액 범위
- 유형 (수입/지출)
- 결제 수단

### 기능 6: AI 지출 패턴 분석 ⭐
AI 지출 분석
- 입력: 최근 거래 내역
- 분석: 지출 패턴, 습관, 트렌드
- 출력: 주요 지출 항목, 불필요한 지출, 절약 가능 항목

### 기능 7: AI 예산 제안 ⭐
AI 예산 추천
- 입력: 수입, 과거 지출 내역
- 분석: 카테고리별 지출 비율
- 출력: 최적 예산 배분 제안

### 기능 8: AI 카테고리 자동 분류 ⭐
AI 자동 분류
- 입력: 거래 설명
- 분석: 키워드, 패턴
- 출력: 적절한 카테고리 제안

### 기능 9: AI 이상 거래 감지 ⭐
AI 이상 탐지
- 입력: 최근 거래
- 분석: 평소 패턴과 비교
- 출력: 이상 거래 알림 (금액, 빈도, 카테고리)

### 기능 10: AI 저축 조언 ⭐
AI 저축 팁
- 입력: 수입, 지출, 저축 목표
- 분석: 현재 재무 상태
- 출력: 저축 전략, 실천 가능한 팁, 예상 저축액

---

## 3. 데이터 구조

### 3.1 테이블 (4개)

#### transactions (거래)
```typescript
{
  id: string (uuid)
  amount: number
  category: string (FK → categories.name)
  description: string
  date: date
  type: enum ('income', 'expense')
  paymentMethod: enum ('cash', 'card', 'transfer')
  createdAt: timestamp
  updatedAt: timestamp
}
```

#### categories (카테고리)
```typescript
{
  id: string (uuid)
  name: string (unique)
  color: string
  icon: string
  monthlyBudget: number | null
  createdAt: timestamp
}
```

#### budgets (예산)
```typescript
{
  id: string (uuid)
  month: string (YYYY-MM)
  totalBudget: number
  categoryBudgets: jsonb (카테고리별 예산)
  createdAt: timestamp
  updatedAt: timestamp
}
```

#### ai_insights (AI 인사이트)
```typescript
{
  id: string (uuid)
  type: enum ('spending_pattern', 'budget_suggestion', 'anomaly_detection', 'savings_advice')
  title: string
  content: text
  metadata: jsonb (추가 정보)
  month: string (YYYY-MM)
  createdAt: timestamp
}
```

---

## 4. AI 구현

### 4.1 AI SDK 설정
```bash
npm install ai @ai-sdk/openai
```

### 4.2 Open Router 설정
```typescript
import { createOpenAI } from '@ai-sdk/openai'

const openrouter = createOpenAI({
  baseURL: 'https://openrouter.ai/api/v1',
  apiKey: process.env.OPENROUTER_API_KEY,
})

const model = openrouter('anthropic/claude-haiku-4.5')
```

### 4.3 AI 함수

#### analyzeSpendingPattern(transactions)
```typescript
입력: {
  transactions: [
    { amount: 50000, category: "식비", date: "2026-01-10" },
    { amount: 30000, category: "교통", date: "2026-01-12" },
    ...
  ],
  month: "2026-01"
}

출력: {
  summary: "이번 달 식비 지출이 전월 대비 25% 증가했습니다.",
  topSpending: [
    { category: "식비", amount: 450000, percentage: 35 },
    { category: "쇼핑", amount: 300000, percentage: 23 }
  ],
  unnecessarySpending: [
    "배달 음식 주 3회 이상 (150,000원)",
    "충동 구매 (80,000원)"
  ],
  savingOpportunities: [
    { item: "배달비 절약", potentialSaving: 60000 },
    { item: "구독 서비스 정리", potentialSaving: 30000 }
  ],
  trends: [
    "주말 지출이 평일 대비 2배 높음",
    "오후 3-6시 카페 지출 집중"
  ]
}
```

#### suggestBudget(income, transactions)
```typescript
입력: {
  income: 3000000,
  pastTransactions: [...] (최근 3개월)
}

출력: {
  totalBudget: 2500000,
  categoryBudgets: {
    "식비": { amount: 500000, percentage: 20, reasoning: "과거 평균 지출 기준" },
    "교통": { amount: 200000, percentage: 8, reasoning: "통근비 고정" },
    "주거": { amount: 800000, percentage: 32, reasoning: "월세 및 관리비" },
    "쇼핑": { amount: 300000, percentage: 12, reasoning: "필수 구매 + 여유" },
    "공과금": { amount: 150000, percentage: 6, reasoning: "계절 변동 고려" },
    "기타": { amount: 200000, percentage: 8, reasoning: "예비비" },
    "저축": { amount: 350000, percentage: 14, reasoning: "수입의 14% 저축 권장" }
  },
  insights: [
    "현재 식비가 과다하므로 20%로 조정 권장",
    "저축 비율을 14%에서 20%로 늘리면 연간 2,160,000원 추가 저축 가능"
  ]
}
```

#### categorizeTransaction(description)
```typescript
입력: {
  description: "스타벅스 아메리카노"
}

출력: {
  suggestedCategory: "식비",
  confidence: 0.95,
  reasoning: "카페 음료는 일반적으로 식비로 분류됩니다."
}

입력: {
  description: "버스 카드 충전"
}

출력: {
  suggestedCategory: "교통",
  confidence: 0.98,
  reasoning: "대중교통 관련 지출입니다."
}
```

#### detectAnomalies(transactions)
```typescript
입력: {
  recentTransactions: [...] (최근 1개월),
  historicalAverage: { 식비: 400000, 쇼핑: 200000, ... }
}

출력: {
  anomalies: [
    {
      transaction: { amount: 800000, category: "쇼핑", date: "2026-01-15" },
      reason: "쇼핑 카테고리 평균 대비 4배 높은 금액",
      severity: "high"
    },
    {
      transaction: { amount: 150000, category: "식비", date: "2026-01-20" },
      reason: "일일 식비 평균 대비 3배 높음",
      severity: "medium"
    }
  ],
  summary: "2건의 이상 거래가 감지되었습니다.",
  recommendation: "고액 지출이 의도적인지 확인하세요."
}
```

#### provideSavingsAdvice(income, expenses, goal)
```typescript
입력: {
  monthlyIncome: 3000000,
  monthlyExpenses: 2400000,
  savingsGoal: 10000000,
  timeframe: 12 (개월)
}

출력: {
  currentSavings: 600000,
  requiredMonthlySavings: 833333,
  gap: 233333,
  feasibility: "도전적이지만 가능합니다.",
  strategies: [
    {
      area: "식비",
      currentSpending: 500000,
      suggestedReduction: 100000,
      tips: [
        "주 2회 외식을 1회로 줄이기",
        "점심 도시락 주 3회",
        "배달 대신 직접 조리"
      ]
    },
    {
      area: "쇼핑",
      currentSpending: 300000,
      suggestedReduction: 100000,
      tips: [
        "충동 구매 전 24시간 대기",
        "필요/원함 구분하기",
        "월 쇼핑 예산 설정"
      ]
    }
  ],
  projectedSavings: {
    "3개월": 2500000,
    "6개월": 5000000,
    "12개월": 10000000
  },
  motivation: "현재 속도로 12개월 후 목표 달성 가능합니다!"
}
```

### 4.4 AI 프롬프트 템플릿

#### 지출 패턴 분석 프롬프트
```
당신은 개인 재무 분석 전문가입니다.

다음 거래 내역을 분석해주세요:

[거래 데이터]
{transactions}

분석 내용:
1. 전체 요약 (한 줄)
2. 상위 지출 카테고리 (금액, 비율)
3. 불필요한 지출 항목
4. 절약 기회 (구체적 금액)
5. 지출 트렌드 및 패턴

YOU MUST respond with ONLY valid JSON.
No markdown code blocks.
No preamble.
Just pure JSON.

JSON 형식:
{
  "summary": "요약",
  "topSpending": [
    { "category": "카테고리", "amount": 금액, "percentage": 비율 }
  ],
  "unnecessarySpending": ["항목1", "항목2"],
  "savingOpportunities": [
    { "item": "항목", "potentialSaving": 절약가능금액 }
  ],
  "trends": ["트렌드1", "트렌드2"]
}
```

#### 예산 제안 프롬프트
```
당신은 재무 설계 전문가입니다.

다음 정보를 바탕으로 최적의 예산을 제안해주세요:

[재무 정보]
월 수입: {income}원
과거 지출: {pastTransactions}

요구사항:
1. 총 예산 (수입의 80-90%)
2. 카테고리별 예산 배분 (금액, 비율, 근거)
3. 저축 목표 (수입의 10-20%)
4. 인사이트 및 조언

YOU MUST respond with ONLY valid JSON.
No markdown code blocks.
No preamble.
Just pure JSON.

JSON 형식:
{
  "totalBudget": 총예산,
  "categoryBudgets": {
    "카테고리": {
      "amount": 금액,
      "percentage": 비율,
      "reasoning": "근거"
    }
  },
  "insights": ["인사이트1", "인사이트2"]
}
```

#### 카테고리 자동 분류 프롬프트
```
당신은 거래 내역 분류 전문가입니다.

다음 거래 설명을 적절한 카테고리로 분류해주세요:

[거래]
설명: {description}

카테고리 선택지:
- 식비 (음식, 카페, 외식)
- 교통 (대중교통, 주유, 택시)
- 쇼핑 (의류, 전자제품, 생활용품)
- 공과금 (전기, 가스, 수도, 통신)
- 주거 (월세, 관리비)
- 의료 (병원, 약국)
- 문화 (영화, 공연, 책)
- 기타

YOU MUST respond with ONLY valid JSON.
No markdown code blocks.
No preamble.
Just pure JSON.

JSON 형식:
{
  "suggestedCategory": "카테고리명",
  "confidence": 0.0-1.0,
  "reasoning": "근거"
}
```

#### 이상 거래 감지 프롬프트
```
당신은 이상 거래 탐지 전문가입니다.

다음 거래 내역에서 평소와 다른 이상 패턴을 찾아주세요:

[최근 거래]
{recentTransactions}

[평균 지출]
{historicalAverage}

이상 기준:
- 평균 대비 2배 이상 금액
- 빈도가 급증한 카테고리
- 새로운 카테고리 출현

YOU MUST respond with ONLY valid JSON.
No markdown code blocks.
No preamble.
Just pure JSON.

JSON 형식:
{
  "anomalies": [
    {
      "transaction": { "amount": 금액, "category": "카테고리", "date": "날짜" },
      "reason": "이유",
      "severity": "high|medium|low"
    }
  ],
  "summary": "요약",
  "recommendation": "권장사항"
}
```

#### 저축 조언 프롬프트
```
당신은 저축 전략 전문가입니다.

다음 재무 상태를 분석하고 저축 목표 달성 전략을 제시해주세요:

[재무 정보]
월 수입: {income}원
월 지출: {expenses}원
저축 목표: {goal}원
기간: {timeframe}개월

요구사항:
1. 현재 저축액 vs 필요 저축액
2. 목표 달성 가능성 평가
3. 카테고리별 절감 전략 (구체적 팁)
4. 단계별 저축 예상액
5. 동기 부여 메시지

YOU MUST respond with ONLY valid JSON.
No markdown code blocks.
No preamble.
Just pure JSON.

JSON 형식:
{
  "currentSavings": 현재저축,
  "requiredMonthlySavings": 필요저축,
  "gap": 부족액,
  "feasibility": "평가",
  "strategies": [
    {
      "area": "영역",
      "currentSpending": 현재지출,
      "suggestedReduction": 절감목표,
      "tips": ["팁1", "팁2"]
    }
  ],
  "projectedSavings": {
    "3개월": 금액,
    "6개월": 금액,
    "12개월": 금액
  },
  "motivation": "동기부여"
}
```

---

## 5. UI 레이아웃 & 인터랙션

### 5.1 메인 레이아웃
```
┌─────────────────────────────────────────────────────────┐
│  💰 Personal Finance          [+ 거래 추가] [프로필]    │
├─────────────────────────────────────────────────────────┤
│  탭: [대시보드] [거래내역] [예산] [카테고리] [인사이트] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                  탭 콘텐츠 영역                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 5.2 대시보드 탭
**화면 구성:**
- 월별 요약 카드 (수입, 지출, 저축, 저축률)
- 예산 사용률 진행바
- 카테고리별 지출 Pie Chart
- 월별 수입/지출 Line Chart

**인터랙션:**
없음 (읽기 전용)

### 5.3 거래내역 탭
**화면 구성:**
- [+ 거래 추가] 버튼
- 검색/필터 바
- 거래 목록 (날짜, 카테고리, 금액, 설명)

**인터랙션 플로우:**
1. [+ 거래 추가] 클릭 → Dialog 열림
2. 폼 입력 (금액, 카테고리, 설명, 날짜, 유형, 결제수단)
3. [AI 분류] 버튼 (선택) → AI가 카테고리 제안
4. [저장] 클릭 → createTransaction() 호출
5. 성공 → Dialog 닫기 + toast.success() + 리페치
6. 실패 → toast.error()

**거래 카드 액션:**
- [편집] → Dialog 열림 (데이터 pre-fill)
- [삭제] → 확인 Dialog → deleteTransaction()

### 5.4 예산 탭
**화면 구성:**
- 월 선택기
- 총 예산 입력
- 카테고리별 예산 목록
- 예산 vs 실제 지출 비교 Bar Chart
- [AI 예산 제안] 버튼

**인터랙션 플로우:**
1. [AI 예산 제안] 클릭 → AI 함수 호출
2. 로딩 상태 표시
3. AI 결과 → 예산 제안 패널 표시
4. [적용] 클릭 → 예산 자동 입력
5. [저장] → saveBudget() 호출

### 5.5 카테고리 탭
**화면 구성:**
- [+ 카테고리 추가] 버튼
- 카테고리 목록 (이름, 색상, 아이콘, 월 예산)

**인터랙션 플로우:**
1. [+ 카테고리 추가] → Dialog
2. 폼 입력 (이름, 색상 선택, 아이콘 선택, 월 예산)
3. [저장] → createCategory() 호출

### 5.6 인사이트 탭
**화면 구성:**
- [AI 분석 실행] 버튼
- 지출 패턴 분석 카드
- 이상 거래 알림
- 저축 조언 카드

**인터랙션 플로우:**
1. [AI 분석 실행] → 4가지 AI 함수 순차 호출
2. 각 AI 결과 → ai_insights 테이블 저장
3. 카드 형태로 표시
4. 새로고침 → 최신 인사이트 로드

---

## 6. 구현 계획

### Phase 1: 프로젝트 셋업 (15분)
- Next.js 프로젝트 생성
- PostgreSQL 연결 (로컬 + 개발)
- Drizzle ORM 설정
- shadcn/ui 설치
- Recharts 설치
- AI SDK + Open Router 설정

### Phase 2: DB & 기본 CRUD (20분)
- 4개 테이블 스키마
- Transaction CRUD
- Category CRUD
- Budget CRUD

### Phase 3: AI 기능 (25분)
- AI SDK 연동
- 5개 AI 함수 구현
- JSON 검증
- 에러 핸들링

### Phase 4: UI 구현 (30분)
- 5개 탭 구현
- Dialog/Sheet
- Charts (Dynamic Import)

### Phase 5: 통합 & 테스트 (10분)
- 검색/필터
- 통계 집계
- E2E 테스트

---

## 7. 환경 변수

### 로컬 개발
```env
DATABASE_URL=postgresql://budget:budget123@localhost:5432/personal_finance
OPENROUTER_API_KEY=your_key_here
```

### 개발계
```env
DATABASE_URL=postgresql://budget:budget123@193.168.195.222:5432/personal_finance
OPENROUTER_API_KEY=your_key_here
```

---

## 8. 테스트 체크리스트

### 8.1 거래 관리
- [ ] 거래 추가/수정/삭제
- [ ] AI 카테고리 분류

### 8.2 예산 관리
- [ ] 예산 설정
- [ ] AI 예산 제안

### 8.3 AI 기능
- [ ] 지출 패턴 분석
- [ ] 이상 거래 감지
- [ ] 저축 조언

### 8.4 통계
- [ ] 차트 정확성
- [ ] 필터 동작

---

## 9. 성공 기준

- ✅ 10개 피처 모두 동작
- ✅ AI 분석 실용적
- ✅ 차트 시각화 명확
- ✅ 반응형 UI
- ✅ 에러 핸들링 완벽
- ✅ 예상 시간: 110분 (실제 130분 예상)

---

**Day 17: AI Personal Finance - 똑똑한 재무 관리! 💰📊**